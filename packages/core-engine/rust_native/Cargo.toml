[package]
name = "rust_native"
version = "0.1.0"
edition = "2021"

[lib]
name = "rust_native"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module"] }
serde = { version = "1.0", features = ["derive"] }
tree-sitter = "0.22"
