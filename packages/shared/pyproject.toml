[tool.poetry]
name = "riskcode-engine-shared"
version = "0.1.0"
description = "Shared utilities for RiskCodeAI"
authors = ["RiskCodeAI Team"]
readme = "README.md"
packages = [{include = "riskcode_shared", from = "src"}]

[tool.poetry.dependencies]
python = "^3.12"
pydantic = "^2.9"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
